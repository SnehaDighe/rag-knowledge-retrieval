apiVersion: v1
kind: Service
metadata:
  name: nodejs-api
  namespace: rag-system
  labels:
    app: nodejs-api
spec:
  type: ClusterIP
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: nodejs-api
